# License Check MCP Server Configuration

# Transport type: 'stdio' (default) or 'http'
LICENSE_CHECK_TRANSPORT=stdio

# HTTP server port (only used when transport is 'http')
LICENSE_CHECK_PORT=3000

# HTTP server host (only used when transport is 'http')
LICENSE_CHECK_HOST=127.0.0.1

# Log level: debug, info, warn, error
LICENSE_CHECK_LOG_LEVEL=info
